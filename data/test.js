




let party = ["r", "d", "i", "total"];

for (i...) {
   creates row
   append row
   let text = [party[i], statics[`number of ${party[i]}`], statics[`${party[i]} vote...`]]
   for (j...) {
       creates col
       append col
       create textNode
       textNode.appendChild(text[j])
   }
}